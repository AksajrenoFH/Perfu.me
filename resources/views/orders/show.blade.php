<x-app-layout><div class="py-10 max-w-4xl mx-auto px-4"><div class="flex justify-between items-center mb-6"><div><h1 class="text-2xl font-black">Pesanan #{{ $order->id }}</h1><p class="text-sm text-gray-500">{{ $order->created_at->format('d M Y, H:i') }}</p></div><a href="{{ route('orders.edit', $order) }}" class="bg-black text-white px-5 py-3 rounded-xl text-sm">Edit</a></div><div class="bg-white border border-gray-100 rounded-2xl p-6 space-y-6"><div class="grid md:grid-cols-2 gap-4"><div><p class="text-xs text-gray-400">Pelanggan</p><p class="font-bold">{{ $order->customer_name ?: 'Belum diisi' }}</p><p>{{ $order->customer_phone }}</p></div><div><p class="text-xs text-gray-400">Status</p><p class="font-bold">{{ $order->status }}</p><p class="mt-2 font-bold">Rp {{ number_format($order->total_price, 0, ',', '.') }}</p></div></div><div><p class="text-xs text-gray-400 mb-2">Item</p>@foreach($order->items as $item)<div class="flex justify-between border-b py-3"><span>{{ $item['name'] }} <span class="text-gray-400">× {{ $item['qty'] }}</span></span><span>Rp {{ number_format($item['price'] * $item['qty'], 0, ',', '.') }}</span></div>@endforeach</div>@if($order->customer_address)<div><p class="text-xs text-gray-400">Alamat</p><p>{{ $order->customer_address }}</p></div>@endif @if($order->notes)<div><p class="text-xs text-gray-400">Catatan</p><p>{{ $order->notes }}</p></div>@endif</div></div></x-app-layout>
