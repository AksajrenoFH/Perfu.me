@if (!request('drawer'))<x-app-layout>@endif
<div class="min-h-screen bg-[#F8F9FA] p-6"><div class="max-w-3xl mx-auto space-y-5"><div class="bg-white rounded-3xl border border-gray-100 p-7"><p class="text-[10px] font-black tracking-[.2em] text-[#D4AF37]">DETAIL BRAND</p><div class="flex gap-5 mt-5 items-center">@if($brand->logo)<img src="{{ asset('storage/'.$brand->logo) }}" class="w-20 h-20 rounded-2xl object-cover">@endif<div><h1 class="text-2xl font-black">{{ $brand->name }}</h1><p class="text-xs text-gray-400 mt-1">/{{ $brand->slug }}</p></div></div><div class="mt-7 border-t pt-5 text-sm text-gray-600 whitespace-pre-line">{{ $brand->description ?: 'Belum ada deskripsi brand.' }}</div></div><a href="{{ route('brands.edit', ['brand'=>$brand, 'drawer'=>request('drawer')]) }}" class="inline-block bg-black text-white px-5 py-3 rounded-xl text-xs font-bold">Edit Brand</a></div></div>
@if (!request('drawer'))</x-app-layout>@endif
